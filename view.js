import inquirer from "inquirer"

function getTable(model){
    const billAmount = 0
    const percentage = "+"
    const tip = 0
    const total = 0
    return[
        {billAmount: billAmount},
    ]
}